# Shell scripts and systemd units run on the Linux board — force LF so a
# commit from a Windows checkout can't introduce CRLF that breaks bash or
# systemd on the device.
*.sh      text eol=lf
*.service text eol=lf
